# RAG-CHATBOAT1
A rag-chatboat that chunks and extracts the text ,builds embeddings and and index into vector db i.e. Pinecone here.Then after hitting the query endpoint it returns the top-k chunks to and return the answer.
